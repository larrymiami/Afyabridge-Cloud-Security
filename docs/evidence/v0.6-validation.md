# v0.6 Application Security Baseline — Validation Evidence

## Validation record

| Field | Value |
|---|---|
| Milestone | v0.6 Application Security Baseline |
| Pull request | PR #6 — Implement v0.6 application security baseline |
| Validated revision | `e3eb19f7aad2c844f38daeb924d6cd0c9b863ed1` |
| Workflow | `Application baseline` |
| Workflow run | #27 |
| Workflow run ID | `31005598423` |
| Result | Success |
| Validation date | 2026-08-05 |

## Successful validation stages

The workflow completed successfully from a clean GitHub-hosted runner with a fresh PostgreSQL 16 service.

- Repository checkout and dependency installation
- Deterministic database migrations
- Synthetic-data seeding
- TypeScript type checking across the workspace
- Authentication, authorization, synchronization, worker, database, and web security tests
- TypeScript package compilation
- Next.js production build and prerendering

## Evidence interpretation

This result validates that the reviewed v0.6 revision can establish its database schema, load synthetic fixtures, pass the implemented automated security tests, and produce the application build in the CI environment.

The evidence does not claim production deployment validation. The local `x-afyabridge-actor` header remains a documented integration seam and must be replaced by a trusted, integrity-protected identity assertion before production use.

## Scope confirmation

- No real patient data is used by the fixtures or tests.
- No production credentials or secrets are required by the workflow.
- Household workflows are implemented and tested.
- Patient and referral workflows remain outside the v0.6 implementation boundary.
- Cloud infrastructure deployment remains part of v0.7.

## Conclusion

The v0.6 application-security controls are **Implemented and validated** for the documented local and CI scope. PR #6 is ready for final review and merge approval.