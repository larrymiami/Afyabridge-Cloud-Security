# v0.7B validation evidence

## Validation target

- **Milestone:** v0.7B — Project factory and country isolation
- **Pull request:** #8
- **Validated source commit:** `0965ade4395c94d46cd35c55f4b86d712faa3f1c`
- **Workflow:** Terraform foundation
- **Workflow run:** #25
- **Run ID:** `31012888832`
- **Result:** Success

## Validated checks

The successful workflow completed the following credential-free checks:

1. `terraform fmt -check -recursive infra/terraform`
2. `terraform -chdir=infra/terraform/bootstrap init -backend=false -input=false`
3. `terraform -chdir=infra/terraform/bootstrap validate -no-color`
4. `terraform -chdir=infra/terraform/environments/foundation init -backend=false -input=false`
5. `terraform -chdir=infra/terraform/environments/foundation validate -no-color`

The validated foundation root composes:

- the shared-services, Kenya, Ghana, and South Africa folder hierarchy;
- environment folders for development, staging, and production;
- authoritative project inventory and generated folder placement;
- default-network suppression and project deletion protection;
- required project labels and controlled API activation;
- additive project IAM with public-principal rejection;
- baseline service-account key creation and upload restrictions;
- optional project budgets and notification thresholds;
- grouped hierarchy, project, IAM, policy, and budget outputs.

## Failure resolved during validation

Workflow run #24 failed only the formatting check for:

```text
infra/terraform/environments/foundation/outputs.tf
```

The assignments in `control_inventory` were reformatted with Terraform's canonical alignment. Commit `0965ade4395c94d46cd35c55f4b86d712faa3f1c` then passed workflow run #25.

## Evidence boundary

This evidence demonstrates successful static Terraform validation. It does **not** demonstrate that resources have been deployed to Google Cloud or that runtime controls are effective.

The following remain unvalidated until a reviewed cloud deployment occurs:

- organization and folder creation;
- project creation and billing attachment;
- effective inherited organization policies;
- IAM propagation and access outcomes;
- API enablement;
- absence of default networks in deployed projects;
- budget creation and alert delivery;
- remote-state access and concurrency;
- cross-country isolation in a live organization;
- post-apply drift behavior.

No live cloud credentials, billing identifiers, organization identifiers, patient data, or production project details were used in pull-request CI.
