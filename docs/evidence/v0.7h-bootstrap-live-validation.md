# v0.7H Terraform Bootstrap Live Validation — validation evidence

## Scope

This evidence record covers the first live Google Cloud validation of the AfyaBridge Terraform infrastructure foundation.

The validation applies only to the protected Terraform bootstrap layer in:

```text
infra/terraform/bootstrap
```

It validates:

- live creation and refresh of the Terraform bootstrap control plane;
- remote state protected by Cloud Storage and Cloud KMS;
- service-account impersonation without a downloaded service-account key;
- provider refresh permissions derived from observed API behavior;
- controlled least-privilege mutation testing;
- consolidation from broad temporary discovery roles into custom steady-state roles;
- removal of temporary discovery and bootstrap privileges;
- migration from authoritative to additive state-bucket IAM ownership after reviewer policy enforcement; and
- a final zero-change Terraform plan after reviewer remediation and IAM cleanup.

This evidence does **not** claim live validation of the foundation, network, workload, observability, edge, GitHub Workload Identity Federation, protected GitHub environments, or country deployment stacks.

## Validation environment

- Validation date: `2026-08-11`
- Branch: `feat/lab1-secure-terraform-bootstrap`
- Google Cloud project: `afyabridge-bootstrap-260808-lm`
- Project number: `49662756213`
- Region: `africa-south1`
- Terraform service account: `terraform-deployer@afyabridge-bootstrap-260808-lm.iam.gserviceaccount.com`
- Terraform state bucket: `afyabridge-tfstate-49662756213`
- Terraform state KMS key: `projects/afyabridge-bootstrap-260808-lm/locations/africa-south1/keyRings/afyabridge-terraform-state/cryptoKeys/terraform-state`

## Authentication boundary

Terraform was executed using Application Default Credentials from an authorized human operator and Google Cloud service-account impersonation.

The effective path was:

```text
Authorized human ADC
        |
        | iam.serviceAccounts.getAccessToken
        v
terraform-deployer
        |
        v
Terraform Google provider
```

No service-account JSON key was created, downloaded, or used.

After bootstrap administration was complete, the operator's temporary project-level IAM grants were removed. A final project IAM query for the human principal returned an empty result while short-lived impersonated credentials remained functional:

```text
RESULT: ADC service-account impersonation succeeded
```

The operator could no longer call `iam.serviceAccounts.getIamPolicy` after the temporary service-account administration grant was removed. This is expected and demonstrates that permission to impersonate the Terraform identity does not imply standing permission to inspect or modify its IAM policy.

## Provider refresh permission discovery

The Terraform Google provider was run under the impersonated `terraform-deployer` identity.

Permissions were derived iteratively from successful provider operations and explicit authorization failures instead of assigning a broad project administration role.

The steady-state bootstrap refresh contract was reduced to:

| Permission | Purpose |
|---|---|
| `resourcemanager.projects.get` | Read bootstrap project metadata required by project-service refresh |
| `serviceusage.services.get` | Read individual Google Cloud service state |
| `serviceusage.services.list` | Enumerate enabled project services |
| `cloudkms.keyRings.get` | Refresh the Terraform state key ring |
| `cloudkms.cryptoKeys.get` | Refresh the Terraform state encryption key |
| `cloudkms.cryptoKeys.getIamPolicy` | Refresh the KMS IAM member managed by Terraform |
| `iam.serviceAccounts.get` | Refresh the Terraform deployment service account |
| `storage.buckets.get` | Refresh state-bucket metadata |
| `storage.buckets.getIamPolicy` | Refresh the state-bucket IAM policy |

During discovery, predefined read-oriented roles were granted only temporarily and with expiration conditions. They included:

```text
roles/browser
roles/serviceusage.serviceUsageViewer
roles/cloudkms.viewer
roles/iam.serviceAccountViewer
roles/iam.securityReviewer
roles/storage.bucketViewer
```

These roles were discovery scaffolding and were removed after the exact steady-state permission contract had been validated.

## Controlled mutation test

A temporary label was selected as a reversible, low-impact mutation of the protected state bucket:

```text
handoff_test = validated
```

The initial Terraform apply was executed without bucket metadata update permission. Terraform reached the Cloud Storage API and failed with:

```text
Permission 'storage.buckets.update' denied
```

This established the negative control:

```text
storage.buckets.get       = sufficient for refresh
storage.buckets.update    = required for bucket metadata mutation
```

A temporary custom role containing only `storage.buckets.update` was then bound specifically to:

```text
projects/_/buckets/afyabridge-tfstate-49662756213
```

Cloud Storage `testIamPermissions` confirmed that the Terraform identity received the exact permission:

```json
{
  "kind": "storage#testIamPermissionsResponse",
  "permissions": [
    "storage.buckets.update"
  ]
}
```

The same Terraform mutation was repeated and succeeded:

```text
Apply complete! Resources: 0 added, 1 changed, 0 destroyed.
```

The temporary label was subsequently removed through Terraform. The revert also completed successfully:

```text
Apply complete! Resources: 0 added, 1 changed, 0 destroyed.
```

A bucket metadata read confirmed that `handoff_test` no longer existed.

## Final steady-state IAM

The discovery roles were replaced with two purpose-specific custom project roles.

### Terraform bootstrap reader

```text
projects/afyabridge-bootstrap-260808-lm/roles/afyabridgeTerraformBootstrapReader
```

Permissions:

```text
resourcemanager.projects.get
serviceusage.services.get
serviceusage.services.list
cloudkms.keyRings.get
cloudkms.cryptoKeys.get
cloudkms.cryptoKeys.getIamPolicy
iam.serviceAccounts.get
storage.buckets.get
storage.buckets.getIamPolicy
```

The reader is bound to:

```text
serviceAccount:terraform-deployer@afyabridge-bootstrap-260808-lm.iam.gserviceaccount.com
```

### Terraform state bucket updater

```text
projects/afyabridge-bootstrap-260808-lm/roles/afyabridgeTerraformStateBucketUpdater
```

Permission:

```text
storage.buckets.update
```

The binding is constrained to:

```text
resource.type == 'storage.googleapis.com/Bucket'
&&
resource.name == 'projects/_/buckets/afyabridge-tfstate-49662756213'
```

The Terraform service account's final project-level IAM inventory therefore contains only:

```text
afyabridgeTerraformBootstrapReader
    -> steady-state bootstrap refresh

afyabridgeTerraformStateBucketUpdater
    -> storage.buckets.update
    -> protected state bucket only
```

Terraform backend object access remains separately governed by the state bucket IAM policy.

## Temporary privilege cleanup

Bootstrap and permission discovery used time-bounded IAM bindings for operations including:

- initial state-bucket administration;
- KMS administration;
- service enablement;
- service-account creation;
- custom-role administration;
- provider permission discovery;
- impersonation recovery; and
- service-account IAM administration.

After validation, the temporary bindings were physically removed from the project IAM policy. The cleanup verification query returned an empty result for all IAM conditions with titles beginning `lab1_`.

The temporary `lab1BucketMetadataUpdater` custom role was also removed. No permanent project-level administrative role remains assigned to the human validation identity.

## Initial drift validation

After reverting the controlled mutation, installing the final custom roles, removing the broad discovery roles, removing temporary human administration grants, and deleting the temporary test role, Terraform refreshed the complete bootstrap stack successfully.

Initial result:

```text
No changes. Your infrastructure matches the configuration.
```

That result established the least-privilege execution handoff for the initial live bootstrap implementation.

## Reviewer policy finding: authoritative bucket IAM

The first PR security-gate reviewer pass then detected a high-severity repository-policy violation:

```text
terraform-no-authoritative-iam-policy
```

The live bootstrap configuration used:

```text
google_storage_bucket_iam_policy.terraform_state
```

This resource owned the entire state-bucket IAM policy. That ownership model could remove unrelated future principals if additional plan/apply identities or state consumers were later added outside the same authoritative policy document.

The reviewer finding was remediated in code by replacing the authoritative policy resource with:

```text
google_storage_bucket_iam_member.terraform_state_object_admin
```

for the single required backend binding:

```text
roles/storage.objectAdmin
serviceAccount:terraform-deployer@afyabridge-bootstrap-260808-lm.iam.gserviceaccount.com
```

A Terraform `removed` block with `destroy = false` was used for the historical authoritative-policy resource so Terraform would discard its state ownership without deleting the live bucket policy.

The reviewer migration plan showed:

```text
Plan: 1 to add, 0 to change, 0 to destroy.
```

and explicitly stated that `google_storage_bucket_iam_policy.terraform_state` would no longer be managed by Terraform but would not be destroyed.

## Reviewer IAM migration validation

The additive IAM migration required one temporary write permission:

```text
storage.buckets.setIamPolicy
```

A temporary custom role was granted only to `terraform-deployer`, only for the protected state bucket, and only for the short reviewer migration window.

Cloud Storage `testIamPermissions` confirmed the exact permission before apply:

```json
{
  "kind": "storage#testIamPermissionsResponse",
  "permissions": [
    "storage.buckets.setIamPolicy"
  ]
}
```

The saved reviewer migration plan was then applied without regeneration.

Result:

```text
Apply complete! Resources: 1 added, 0 changed, 0 destroyed.
```

Post-apply validation confirmed all of the following:

- Terraform state contains the additive `google_storage_bucket_iam_member.terraform_state_object_admin` resource and no longer tracks `google_storage_bucket_iam_policy.terraform_state`;
- the live bucket IAM policy still contains `roles/storage.objectAdmin` for `terraform-deployer`;
- the temporary `storage.buckets.setIamPolicy` permission was removed after migration;
- the temporary reviewer custom role and temporary human Role Admin grant were removed;
- the project IAM cleanup query again returned no remaining `lab1_*` conditional bindings; and
- a final Terraform plan after reviewer cleanup reported no changes.

Final reviewer-hardened result:

```text
No changes. Your infrastructure matches the configuration.
```

This closes the reviewer finding with a live, non-destructive ownership migration rather than weakening or bypassing the repository policy gate.

## Security conclusions

The live validation demonstrated that:

1. Terraform can operate through short-lived service-account impersonation without a service-account key.
2. Provider read requirements can be measured rather than approximated with broad predefined roles.
3. Read capability and mutation capability can be separated.
4. `storage.buckets.update` was proven through a denied-before / allowed-after A/B test.
5. Bucket mutation capability can be constrained to the single protected Terraform state bucket.
6. Temporary bootstrap and discovery privileges can be removed without breaking Terraform refresh.
7. The human operator does not retain project-level administration after bootstrap.
8. Repository policy enforcement can identify a live IAM ownership risk before merge.
9. An authoritative bucket IAM policy can be migrated non-destructively to additive member ownership.
10. Temporary IAM-policy write privilege can be granted for the migration and removed afterward without breaking steady-state refresh.
11. The reviewer-hardened Terraform configuration is drift-free after IAM consolidation and ownership migration.

## Evidence retained locally

The validation session retained local evidence including:

```text
refresh-only.txt
mutation-plan.txt
mutation-apply.txt
mutation-apply-success.txt
mutation-revert-apply.txt
final-clean-plan.txt
final-custom-role-plan.txt
post-cleanup-final-plan.txt
iam-before-cleanup.json
iam-pre-final-cleanup.json
iam-after-cleanup.json
iam-final.json
remove-temp-bindings-run.txt
scoped-impersonation-proof.txt
reviewer-iam-migration-plan.txt
reviewer-iam-migration.tfplan
reviewer-iam-migration-apply.txt
reviewer-bucket-iam-after-migration.json
iam-reviewer-final.json
reviewer-post-cleanup-final-plan.txt
```

Raw Terraform state, access tokens, credentials, and service-account keys are not committed as evidence.

## Remaining v0.7 live-validation boundary

This milestone validates only the Terraform bootstrap control plane.

The following remain pending:

- GitHub Workload Identity Federation token exchange;
- repository variables and federation configuration;
- protected deployment environments and approval enforcement;
- foundation project and folder creation;
- organization-policy effectiveness;
- Shared VPC and country-isolation behavior;
- workload deployment;
- CMEK behavior outside the bootstrap state boundary;
- observability and detection delivery;
- public edge, DNS, certificate, and Cloud Armor behavior; and
- rollback and recovery exercises for later Terraform roots.

These controls must retain their existing **implemented/static-validation only** status until separately exercised in live Google Cloud environments.
