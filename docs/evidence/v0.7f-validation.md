# v0.7F static validation evidence

## Scope

This evidence covers the centralized observability, Cloud Monitoring, and initial security-detection Terraform introduced in v0.7F.

## Reviewed revision

- Branch: `feat/v0.7f-centralized-observability`
- Static-validation commit: `b73e1b77ee31018a81b64d330ec97dcc38267892`
- Workflow: `Terraform foundation`
- Run number: `#197`
- Run ID: `31151479789`
- Result: `success`

## What was validated

The workflow completed recursive `terraform fmt -check -diff`, backend-free initialization, and `terraform validate` for the repository's Terraform roots, including `infra/terraform/environments/observability`.

The successful observability validation exercises the provider schema for the centralized log-bucket and organization-sink resources, sink-writer IAM, Monitoring notification channels, Monitoring alert policies, project log-based metrics, and detection alert-policy configuration as represented in this revision.

## Implemented control surface

v0.7F contains:

- a reusable centralized-logging module;
- a country-aware observability environment;
- central administrative and selected security audit routing;
- country-local operational log routing for Kenya, Ghana, and South Africa;
- configurable retention and optional security-bucket retention locking;
- reusable Monitoring notification-channel and threshold-policy primitives;
- initial Cloud Run, Cloud SQL, and logging-pipeline alert contracts;
- reusable project log-based security-detection metrics and alerts;
- initial detections for service-account keys, primitive IAM roles, public IAM, Logging changes, KMS changes, and Secret Manager access denial;
- detection catalogue and responder runbook.

## What this evidence does not prove

No Terraform apply was performed for v0.7F as part of this evidence. No Google Cloud organization, logging project, bucket, sink, notification channel, metric, alert policy, or detection was created or inspected live.

This run does not prove audit-log availability, sink delivery, data residency, IAM effectiveness, metric increments, alert opening or auto-close, notification delivery, responder escalation, false-positive behavior, cross-country isolation, retention enforcement, or recovery behavior.

The log-based metrics are not retroactive. Runtime validation therefore requires creating the metrics first and then generating controlled test events.

## Required live evidence

A future deployment-validation package should capture reviewed Terraform plans and applies, destination bucket inventory, sink writer identities and permissions, sample routed audit logs, country-isolation checks, notification-channel test delivery, operational alert tests, positive and negative detection tests, incident metadata, alert recovery, and any residual exceptions.

## Evidence classification

**Control state:** Implemented and statically validated.  
**Runtime state:** Not deployed and not runtime validated.
