# v0.10B Cloud Posture and Drift Checks — validation evidence

## Scope

This evidence record covers the repository-executable portion of v0.10B on pull request #25. It validates executable posture-rule governance, repository desired-state collection, posture decisions, source/collector compromise behavior, Terraform drift decision logic, CI evidence generation, and propagation into the protected-main `Security gate verdict`.

It does not represent live Google Cloud posture or live remote-state drift evidence.

## Reviewed implementation validation

- Branch: `feat/v0.10b-posture-drift-checks`
- Final security-logic commit before documentation reconciliation: `cf7706699546de18e68ddb0ee2e19ef58cb85f43`
- Terraform foundation workflow: run #319 (`31190676330`) — **success** across all seven Terraform roots
- Application baseline workflow: run #180 (`31190676725`) — **success**
- Supply chain workflow: run #122 (`31190676715`) — unprivileged build/SBOM/evidence job **success**; signing/provenance skipped as designed on a pull request
- Security gates workflow #152 (`31190676150`) — `Security governance validation` **success** on the final security-logic commit; the longer aggregate run was subsequently superseded by a documentation-only revision before its final verdict completed

A prior reviewer candidate, Security gates #150, completed the full scanner/policy/evidence suite and `Security gate verdict` successfully before the final edge-logging source guard was added. The final PR-head aggregate result is recorded in the pull-request review summary rather than rewriting this implementation-specific evidence every time documentation is reconciled.

## v0.10A baseline retained

The v0.10A catalogue validation remained green:

```text
Cloud posture catalogue validated: 18 controls across 9 categories.
Control-matrix lifecycle: {"Implemented":13,"In progress":5}
Validation modes: {"repository-static":8,"ci-enforced":7,"live-pending":3}
Severity profile: {"critical":6,"high":11,"medium":1}
Controls still requiring live Google Cloud validation: 15.
Cloud posture negative controls validated: 15 scenarios passed.
```

v0.10B therefore extends the governed baseline rather than replacing it.

## Executable rule governance

The final executable repository check set was validated as:

```text
Cloud posture rules validated: 11 rules, 42 assertions, 11 anchored rule/control bindings.
Cloud posture rule governance validated: 10 scenarios passed.
```

The rule-governance tests demonstrate fail-closed behavior for:

- deletion of a required rule;
- remapping a required rule to a lower-severity control;
- duplicate rule IDs;
- unsupported live-cloud source activation;
- a claim scope that overstates live effective state;
- removal of all assertions from a rule;
- an unsupported assertion operator;
- deletion of both a rule and its governance binding in the same mutation; and
- governance remapping of a required rule to a lower-severity control.

This reviewer hardening was added after recognizing that an ungoverned rule list could otherwise lose coverage while still producing a superficially clean summary.

## Repository posture evaluation

The normalized desired-state snapshot evaluated cleanly:

```text
Cloud posture evaluated: 11/11 rules passing; 0 blocking finding(s); 11 rule(s) still require live validation.
```

The final phrase is material: passing repository rules prove the reviewed desired-state invariants represented by those rules. They do not prove deployed Google Cloud effective state.

Current repository rules cover selected IAM, Cloud Run identity/ingress/invoker controls, Cloud SQL private networking, edge/WAF/TLS/DNS controls, KMS safeguards, Secret Manager usage, storage public-access safeguards, project governance metadata, centralized logging plumbing, and country/environment scope guardrails.

## Source and collector compromise validation

The source/collector suite copies the Terraform tree into isolated fixtures, mutates reviewed contracts, and re-runs the real collector or the specific source guard being reviewed. The final security-logic commit produced:

```text
Repository posture collector compromise controls validated: 21 scenarios passed.
```

The suite demonstrates detection of changes including:

- service-account key policy weakening;
- removal of the non-disableable service-account-key policy input guardrail;
- service-account key resources;
- primitive IAM roles;
- direct singular and plural public IAM principals;
- Terraform-managed secret payload versions;
- Cloud SQL public IPv4;
- unrestricted Cloud Run ingress;
- removal of the public-invoker guardrail;
- storage public-access-prevention weakening;
- Cloud Armor detachment;
- removal of KMS rotation/destruction defaults;
- shortening the reviewed 90-day KMS rotation default to 60 seconds;
- shortening the reviewed 30-day KMS scheduled-destruction delay to one day;
- weakening workload-level KMS override guardrails;
- project owner metadata removal;
- centralized organization logging-sink removal;
- weakening backend request-log sampling so zero becomes permitted;
- commented-out safe Terraform being ignored when the active value is unsafe; and
- generated `.terraform` cache content being excluded from posture facts.

The HCL source preprocessor preserves quoted strings while stripping `#`, `//`, and block comments before source matching, preventing commented-out safe patterns from spoofing compliance.

The collector remains a focused source-pattern collector over reviewed Terraform contracts, not a general HCL interpreter and not an effective-state engine. Terraform validation, Trivy, OPA, and later live-state collection remain independent layers.

## Terraform input guardrails added during review

The adversarial reviewer pass found a class of fail-open desired-state risk: a module could have a safe default while its caller was still allowed to override that default to an unsafe value.

The remediation was made in the Terraform contracts themselves rather than only in the observer:

- foundation input validation now requires both `iam.disableServiceAccountKeyCreation` and `iam.disableServiceAccountKeyUpload` to remain enabled;
- workload KMS input validation requires the reviewed 90-day rotation and 30-day scheduled-destruction contract;
- edge backend-log sampling must remain greater than zero, so an enabled log block cannot be rendered operationally silent by setting its sample rate to `0`.

Terraform foundation #319 validated these changes across bootstrap, foundation, network, workloads, federation, observability, and edge roots.

The review deliberately did **not** freeze every operational tuning value. For example, Cloud Armor rate-limit thresholds remain tunable because the current posture claim is that a throttling control is present; a zero log sample rate was constrained because it would directly contradict the claim that backend request logging is enabled.

## Evaluator decision validation

The normalized-fact evaluator suite produced:

```text
Cloud posture evaluator controls validated: 20 scenarios passed.
```

Critical and High mutations were confirmed as blocking. The Medium `GOV-C01` project-owner metadata mutation was deliberately confirmed as a report-only finding under the current severity policy, demonstrating that evaluator behavior follows governed merge-blocking policy rather than treating every finding identically.

## Terraform drift decision validation

The Terraform plan evaluator suite produced:

```text
Terraform drift evaluator controls validated: 6 scenarios passed.
```

Validated decisions were:

- no-op/read plan — clean;
- managed-resource update — High;
- unexpected managed-resource creation — High;
- managed-resource deletion — Critical;
- managed-resource replacement — Critical; and
- data-source read — ignored.

These are decision-logic tests against synthetic `terraform show -json` structures. A real drift claim requires a plan generated against deployed remote state with no intentional configuration change.

## Reviewer findings retained as evidence

### Severity-policy test mismatch

The first v0.10B Security governance execution failed inside the new evaluator negative suite after the real repository posture evaluation itself had passed.

The failed test expected removal of a project ownership label to be merge-blocking. The mapped `GOV-C01` control is Medium under the governed severity policy, where `merge_blocking` is false. The evaluator was correct; the test harness was wrong.

The test was corrected to assert the governed blocking decision explicitly rather than weakening or special-casing the evaluator. The final suite proves both blocking and report-only behavior.

### Executable-rule coverage fail-open risk

The reviewer pass found that an ungoverned executable rule list could be reduced or remapped while still producing a superficially clean result. v0.10B therefore added anchored required rule/control bindings and negative tests for deletion, remapping, unsupported source/scope, empty assertions, and simultaneous rule+governance deletion.

### Source-collector trust hardening

The reviewer pass separated collector/source compromise tests from normalized-fact evaluator tests so both layers are independently exercised. It also excluded generated `.terraform` cache content, added singular/plural public-principal coverage and Cloud Run public-invoker validation, stripped Terraform comments before source matching, and tightened KMS defaults from “some duration exists” to the reviewed 90-day rotation and 30-day scheduled-destruction values.

### Safe-default / unsafe-override risk

The reviewer pass found that safe module defaults were insufficient evidence where callers could still supply unsafe values. Foundation service-account-key policies and workload KMS durations were therefore made non-weakenable through the reviewed input contracts. The same sweep found that edge backend logging could be effectively disabled with a zero sample rate; that input is now constrained above zero and covered by a mutation test.

## CI evidence

`Security gate evidence` includes and uploads for 30 days:

- `cloud-posture-snapshot.json`;
- `cloud-posture-evaluation.json`;
- `cloud-posture-evaluation.md`; and
- the existing machine-readable and human-readable security-gate evidence.

The posture Markdown summary is appended to the GitHub Actions job summary. A blocking Critical/High posture finding causes the governance job to fail, which propagates to the same `Security gate verdict` required by the protected `main` ruleset.

## Current boundary

v0.10B currently validates:

- governed executable repository posture coverage;
- normalized desired-state collection from reviewed Terraform contracts;
- fail-closed posture decisions for Critical/High findings;
- report-only handling where governance explicitly defines a non-blocking severity;
- source/collector compromise behavior;
- reviewed Terraform input guardrails for selected security invariants;
- machine-readable and reviewer-readable posture evidence; and
- Terraform drift decision logic with synthetic plans.

It does **not** prove:

- Cloud Asset Inventory collection or queries;
- Security Command Center enablement or findings;
- effective live organization policies or IAM bindings;
- live DNS, network, KMS, Secret Manager, storage, database, logging, or resource-location posture;
- a Terraform plan generated against deployed remote state in drift mode;
- a real out-of-band console/manual change and reconciliation; or
- a real cloud finding progressing through remediation and closure.

Those validations remain dependent on the pending live v0.7 Google Cloud deployment path and later v0.10/v0.11 operational testing.
